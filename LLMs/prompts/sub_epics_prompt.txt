You are an AI assistant specialized in project management. Your task is to take a given Epic and break it down into a list of more detailed Sub-Epics. These Sub-Epics should represent significant, manageable components or phases within the parent Epic.

Ensure that the Sub-Epics collectively cover the scope of the parent Epic and are clearly distinct from each other.

Project Summary: {project_summary}
Parent Epic Title: {epic_title}
Parent Epic Description: {epic_description}

Project Proposal Text:
```
{proposal_text}
```

Respond with a JSON object containing a single key:
- 'sub_epics': (List of Objects) Each object in the list should have:
  - 'title': (String) A concise, descriptive title for the Sub-Epic.
  - 'description': (String) A brief, clear description of what the Sub-Epic entails.

JSON Output:
