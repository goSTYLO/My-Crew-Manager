# FastAPI + server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# HuggingFace + LangChain
transformers>=4.45.0
huggingface_hub>=0.24.0
langchain>=0.3.0
langchain-huggingface>=0.1.0
langchain-community>=0.3.0

# PyTorch with CUDA 12.8 (RTX 4050, driver 581+). Install before other deps:
#   pip uninstall torch -y
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# CPU-only fallback: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
# Must match torch version (0.25 for torch 2.10) to avoid "operator torchvision::nms does not exist"
torchvision>=0.25.0,<0.26

# LLM dependencies
openai>=1.0.0
google-genai>=1.0.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.10.0
accelerate>=0.33.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# 4-bit quantization (Linux/CUDA)
bitsandbytes>=0.43.0; platform_system == "Linux"

# Jupyter (for future model fine-tuning). Install in root .venv:
jupyter>=1.0.0
